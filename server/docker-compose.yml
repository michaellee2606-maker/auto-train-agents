version: '3.8'

services:
  langgraph-agent:
    build: .
    ports:
      - "2024:2024"
    volumes:
      - .:/app
    environment:
      - HUGGINGFACEHUB_API_TOKEN=${HUGGINGFACEHUB_API_TOKEN}
      - LANGSMITH_API_KEY=${LANGSMITH_API_KEY}
    env_file:
      - .env